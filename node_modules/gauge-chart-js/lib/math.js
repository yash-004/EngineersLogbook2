"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// https://en.wikipedia.org/wiki/B%C3%A9zier_curve#Cubic_B%C3%A9zier_curves
function cubicBezier(x1, y1, x2, y2) {
    return function (timeFraction) {
        return (Math.pow(1 - timeFraction, 3) * x1 +
            3 * Math.pow(1 - timeFraction, 2) * timeFraction * y1 +
            3 * (1 - timeFraction) * Math.pow(timeFraction, 2) * x2 +
            Math.pow(timeFraction, 3) * y2);
    };
}
exports.cubicBezier = cubicBezier;
function calcCoordinatesFromAngle(radius, angle) {
    var theta = Math.PI - (angle * Math.PI) / 180;
    return {
        x: radius * Math.sin(theta),
        y: radius * Math.cos(theta)
    };
}
exports.calcCoordinatesFromAngle = calcCoordinatesFromAngle;
