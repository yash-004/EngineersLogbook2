"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var math_1 = require("./math");
describe('calCoordinatesFromAngle', function () {
    it('can calculate coordinates from an angle', function () {
        var point = math_1.calcCoordinatesFromAngle(5, 90);
        expect(point.x).toBe(5);
        expect(point.y).toBeLessThan(1);
        expect(point.y).toBeGreaterThanOrEqual(0);
        var point2 = math_1.calcCoordinatesFromAngle(10, 180);
        expect(point2.y).toBe(10);
        expect(point2.x).toBeLessThan(1);
        expect(point2.x).toBeGreaterThanOrEqual(0);
    });
});
